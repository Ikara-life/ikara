---
import {Button} from "./button";
---

<div class="relative z-30 text-center px-6">
    <h1 class="text-4xl sm:text-6xl lg:text-7xl font-bold mb-7 tracking-tight text-gradient-modern leading-tight">
        A New Rhythm for Living
    </h1>
    <p class="text-lg sm:text-xl max-w-2xl mx-auto mb-10 text-white/90">
        In a world moving too fast, Ikara is a gentle pause — a compass guiding you toward what really
        matters.
    </p>
    <div
            class="flex flex-col sm:flex-row gap-4 justify-center animate-fade-in [animation-delay:0.4s]">
        <Button
                variant="default"
                size="lg"
                className="bg-white text-gray-900 hover:bg-gray-50"
        >
            Apply Now
        </Button>
        <Button
                variant="outline"
                size="lg"
                className="text-white border-white hover:bg-white/10"
        >
            Explore More
        </Button>
    </div>
</div>
