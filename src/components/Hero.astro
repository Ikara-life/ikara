---
import HeroButtons from './ui/HeroButtons.astro';
import {Image} from 'astro:assets';
import heroBg from '../images/hero-bg.webp';
---

<!-- Preload full-res image -->
<link rel="preload" as="image" href={heroBg.src}/>

<section
    class="relative w-full min-h-screen flex items-center justify-center ikara-gradient text-white px-4 pt-40 pb-20">

  <!-- Low-res blurred image placeholder -->
  <div class="absolute inset-0 z-0">
    <Image
        src={heroBg}
        alt="Blurred background"
        class="w-full h-full object-cover filter blur-2xl scale-105"
        loading="eager"
        decoding="async"
        sizes="100vw"
    />
  </div>

  <!-- Full-res image with fade-in -->
  <div class="absolute inset-0 z-10 transition-opacity duration-1000 opacity-0" id="hero-full">
    <Image
        src={heroBg}
        alt="Hero background"
        class="w-full h-full object-cover brightness-75"
        loading="eager"
        decoding="async"
        sizes="100vw"
        onload="document.getElementById('hero-full').classList.remove('opacity-0')"
    />
  </div>

  <!-- Gradient overlay -->
  <div class="absolute inset-0 bg-gradient-to-b from-black/20 to-black/20 z-10"></div>

  <!-- Hero content -->
  <div class="relative z-30 text-center px-6 animate-fade-in-up">
    <h1 class="text-4xl sm:text-6xl lg:text-7xl font-bold mb-7 tracking-tight text-gradient-modern leading-tight">
      A New Rhythm for Living
    </h1>
    <p class="text-lg sm:text-xl max-w-2xl mx-auto mb-10 text-white/90">
      In a world moving too fast, Ikara is a gentle pause — a compass guiding you toward what really
      matters.
    </p>
    <HeroButtons/>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-6 left-1/2 transform -translate-x-1/2 z-30 animate-bounce">
    <svg
        class="w-6 h-6 text-white opacity-70"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
    >
      <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M19 14l-7 7m0 0l-7-7m7 7V3"
      />
    </svg>
  </div>
</section>

<style>
  .text-gradient-modern {
    background: linear-gradient(to right, #a0aec0, #edf2f7, #f7fafc);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 1s ease-out both;
  }
</style>
