---
import HeroButtons from './ui/HeroButtons.astro';
import { Image } from 'astro:assets';
import heroBg from '../images/hero-bg.webp';
---

<!-- Preload the hero image -->
<link rel="preload" as="image" href={heroBg.src} />

<section class="hero-bg min-h-screen flex items-center justify-center relative overflow-hidden pt-20">
  <!-- Background with gradient overlay -->
  <div class="absolute inset-0 bg-gradient-to-b from-black/60 to-black/40"></div>

  <!-- Low resolution placeholder -->
  <div class="absolute inset-0 bg-gray-900 blur-xl"></div>

  <!-- Main image with blur-up effect -->
  <div class="absolute inset-0 transition-opacity duration-1000 opacity-0" id="hero-image">
    <Image
      src={heroBg}
      alt="Hero background"
      class="w-full h-full object-cover"
      loading="eager"
      format="webp"
      decoding="async"
      width={1920}
      height={1080}
      sizes="100vw"
      onload="document.getElementById('hero-image').classList.remove('opacity-0')"
    />
  </div>

  <div class="container relative z-10 text-center px-4">
    <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 text-gradient">
      A New Rhythm for Living
    </h1>
    <p class="hero-text text-white/90 mb-12 max-w-3xl mx-auto">
      In a world moving too fast, Ikara is a gentle pause — a compass guiding you toward what really
      matters.
    </p>
    <HeroButtons/>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <svg
        class="w-6 h-6 text-white"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
    >
      <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M19 14l-7 7m0 0l-7-7m7 7V3"
      />
    </svg>
  </div>
</section>

<style>
  .text-gradient {
    background: linear-gradient(to right, #fff, #e2e8f0);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .hero-text {
    font-size: 1.25rem;
    line-height: 1.75rem;
  }

  @media (min-width: 768px) {
    .hero-text {
      font-size: 1.5rem;
      line-height: 2rem;
    }
  }
</style>
